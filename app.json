{
  "name": "RTMP",
  "keywords": [
    "node",
    "FFmpeg"
  ],
  "stack": "container",
  "env": {
    "KEYRTMP": {
      "description": "RTMP server url with key",
      "required": true
    },
    "URLSRC": {
      "description": "Src to Broadcast",
      "required": true
    }
  }
}
